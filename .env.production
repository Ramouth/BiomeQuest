# BiomeQuest Production Environment Configuration
# Copy this file and fill in the values for your deployment

# NOTE: NODE_ENV is set by docker-compose, not here (Vite doesn't support NODE_ENV in .env)
PORT=3001

# IMPORTANT: Generate a secure random key for JWT signing
# You can generate one with: openssl rand -base64 32
JWT_SECRET=<generate-secure-key>

# Your Hetzner server IP address
SERVER_IP=<your-hetzner-ip>

# Frontend URL (used for CORS configuration)
FRONTEND_URL=http://<your-hetzner-ip>:3001
