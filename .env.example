# BiomeQuest Environment Variables

# Server port (default: 3001)
PORT=3001

# JWT Secret for authentication (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-me

# Domain for HTTPS (Caddy will auto-provision SSL certificate)
# For IP-based access without a domain, use sslip.io:
# Example: If your server IP is 1.2.3.4, use: 1-2-3-4.sslip.io
# For custom domain: biomequest.example.com
DOMAIN=YOUR_DOMAIN_HERE

# Frontend URL for CORS (should match your HTTPS domain)
FRONTEND_URL=https://YOUR_DOMAIN_HERE
